N = 10
VAULT_FILE = "data/vault.json"
STORE_DIR = "store" # This will now be conceptually owned by S00
REGISTRY_FILE = "data/registry.json"
TMP_DIR = "/tmp" # Fallback, but machines will use CLUSTER_ROOT/<ID>/tmp
LEDGER_DIR = "ledger"
CLUSTER_ROOT = "cluster_env"
